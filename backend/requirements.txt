# Core API
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2

# DB / migrations
SQLAlchemy>=2.0
alembic>=1.13
# psycopg[binary] is used for Postgres in prod; SQLite (stdlib) is used for dev/test.
psycopg[binary]>=3.1; platform_system != "Windows"

# Disclosure / labeling
Pillow>=10.2

# Queue / workers (interfaces only in this scaffold)
celery>=5.3
redis>=5.0

# Tests
pytest>=8.0
httpx>=0.27

# NOTE (sandbox honesty):
# - Real Content Credentials would use `c2pa-python`; here ProvenanceService
#   implements a verifiable HMAC-signed manifest behind the same interface.
# - Real generation would use `diffusers`/`torch` (GPU); here a StubGenerationProvider
#   satisfies the GenerationProvider interface.
