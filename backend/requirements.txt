# Core API
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9

# DB / migrations
SQLAlchemy>=2.0
alembic>=1.13
# psycopg[binary] is used for Postgres in prod; SQLite (stdlib) is used for dev/test.
psycopg[binary]>=3.1; platform_system != "Windows"

# Disclosure / labeling / provenance
Pillow>=10.2
# Real C2PA Content Credentials (embed + verify). The HMAC backend is used when
# this is absent; set SCS_PROVENANCE_BACKEND=c2pa to use real Content Credentials.
c2pa-python>=0.32
# Used to mint dev signing certs for C2PA (and present in most stacks already).
cryptography>=42.0

# Queue / workers (interfaces only in this scaffold)
celery>=5.3
redis>=5.0

# Tests
pytest>=8.0
httpx>=0.27

# Optional production extras (NOT installed in the build sandbox):
# Real per-character diffusion generation needs a GPU + these. Without them the
# studio uses StubGenerationProvider; set SCS_GENERATION_PROVIDER=diffusion to use them.
#   torch>=2.2
#   diffusers>=0.27
#   transformers>=4.40
#   accelerate>=0.30
#
# httpx (already above) powers the official Instagram Graph API adapter.
