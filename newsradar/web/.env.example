# Base URL of the NewsRadar FastAPI backend. Server Components read API_BASE;
# the browser reads NEXT_PUBLIC_API_BASE (falls back to same-origin "/api" proxy
# during local dev if unset).
API_BASE=http://localhost:8000
NEXT_PUBLIC_API_BASE=http://localhost:8000
